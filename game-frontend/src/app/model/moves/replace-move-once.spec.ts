import { ReplaceMoveOnce } from './replace-move-once';

describe('ReplaceMoveOnce', () => {
  it('should create an instance', () => {
    expect(new ReplaceMoveOnce(null, null, null)).toBeTruthy();
  });
});
